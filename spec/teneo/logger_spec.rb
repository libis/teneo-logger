# frozen_string_literal: true

RSpec.describe Teneo::Logger do
  it "has a version number" do
    expect(Teneo::Logger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
